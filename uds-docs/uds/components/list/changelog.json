{
  "$schema": "../../schemas/changelog.schema.json",
  "addedIn": "0.2",
  "entries": [
    {
      "version": "0.2",
      "type": "added",
      "text": "Vertical list with single-select, optional leading/trailing icons, keyboard navigation; 5 interaction states"
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "Figma list coverage confirmed: 1-10 item lists, draggable and non-draggable item variants, leading/trailing icons, and default/hover/active/focused/selected states"
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "Replaced documented examples, implementation reference, and playground output with the Web Component API."
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "Cleared legacy per-component behavior references; behavior now lives in the shared Web Components bundle."
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "Ported the full list CSS into `<udc-list>` + `<udc-list-item>` shadow DOM (48px row height, BEM `.udc-list-item__leading-icon` / `__label` / `__trailing-icon` with `--uds-font-size-2xl` icons, hover/active/selected/focus treatments matching `surface-interactive-*` tokens, padding-left/right shrinks when icons are present). Selectable lists expose `role=\"listbox\"` and items get `role=\"option\"`. Rebuilt the playground with items, leading/trailing icon toggles, selected item, and selectable controls."
    }
  ]
}
