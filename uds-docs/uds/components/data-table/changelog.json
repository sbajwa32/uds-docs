{
  "$schema": "../../schemas/changelog.schema.json",
  "addedIn": "0.2",
  "entries": [
    {
      "version": "0.2",
      "type": "added",
      "text": "Data grid with sortable headers, checkbox selection, badge/action cell types, striped rows, error highlighting"
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "Figma data-table coverage expanded: text/dropdown/ghost-button/checkbox/badge cell types, header cells, 1-14 columns, 2-14 rows, alignment, leading/trailing icons, dividers, prominent text, and error highlighting"
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "Replaced documented examples, implementation reference, and playground output with the Web Component API."
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "Cleared legacy per-component behavior references; behavior now lives in the shared Web Components bundle."
    },
    {
      "version": "0.3",
      "type": "changed",
      "text": "`<udc-data-table>` now renders the table itself in shadow DOM driven by `columns` + `rows` JSON attributes (rather than expecting consumers to author the inner `<table>` markup). Ports the original data-table.css inside shadow DOM — 56px sticky header row with surface-alt bg + bold text, 44px body rows with hover/selected/striped surfaces, alignment helpers, prominent text, optional checkbox column with select-all, sortable headers with Material Symbols indicator. Emits `udc-sort-change` and `udc-selection-change`."
    }
  ]
}
