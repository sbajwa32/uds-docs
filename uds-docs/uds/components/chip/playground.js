const html = "<udc-chip variant=\"filter\" selected>Active</udc-chip>\n<udc-chip variant=\"input\" removable>Leaseholder</udc-chip>";

export default {
  controls: [],
  render() {
    return { html, code: html };
  },
};
