import { NotYetPorted } from '@/components/site/NotYetPorted';

export const metadata = { title: 'Composition Recipes — UDS' };

export default function recipes_Page() {
  return <NotYetPorted title="Composition Recipes" chunkRef="06a" />;
}
