import { NotYetPorted } from '@/components/site/NotYetPorted';

export const metadata = { title: 'Semantic Colors — UDS' };

export default function semantic_colors_Page() {
  return <NotYetPorted title="Semantic Colors" chunkRef="06a" />;
}
