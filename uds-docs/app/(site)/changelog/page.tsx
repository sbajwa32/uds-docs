import { NotYetPorted } from '@/components/site/NotYetPorted';

export const metadata = { title: 'Changelog — UDS' };

export default function changelog_Page() {
  return <NotYetPorted title="Changelog" chunkRef="06a" />;
}
