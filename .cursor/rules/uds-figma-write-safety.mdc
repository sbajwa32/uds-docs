---
description: Prevents accidental Figma mutations during UDS sync work. Read-only is the default; writes are explicitly scoped and summarized; component edits round-trip through the component contract and the current factory bar.
alwaysApply: true
lastUpdated: 2026-06-25T21:03:47Z
---

# UDS Figma Write Safety

## Default posture

Figma reads are allowed for UDS work. Figma writes are denied unless the
user explicitly requested the write scope or a skill's instructions require
it after confirmation.

Do not mutate Figma while discovering, auditing, inspecting, or diffing.
Discovery agents are read-only.

## Page-target safety (preflight + postflight for every write)

`figma.currentPage` is NOT a safe write target. It persists from whatever
page the last read or inspection touched, so it can silently point at a
protected page (a `UDS Version: X.Y` page, a live component page, a
`_support` / `📗 Cover` / `🎬 Demo` / `🛝 Playground` page) while you
believe you're on the authorized draft. Creating nodes against
`figma.currentPage` — or `page.appendChild(node)` where
`page = figma.currentPage` — is how an in-scope task leaks an out-of-scope
write. (This rule exists because a stepper-draft task created a proposal
frame on the `UDS Version: 0.3` page: the build script used
`figma.currentPage`, which was still pointing at the version page from an
earlier version-detection read.)

Required BEFORE any node creation or mutation:

1. **Resolve the target page explicitly by name or ID — never read it from
   `figma.currentPage`.** Fetch the page node
   (`figma.root.children.find(p => p.name === '<exact name>')` or
   `getNodeByIdAsync('<pageId>')`), then either
   `await figma.setCurrentPageAsync(targetPage)` or append created nodes
   directly to that resolved page node — not to `figma.currentPage`.
2. **Assert the resolved page is in an allowed write scope before the first
   write.** If the page name doesn't match the authorized scope (e.g. it
   lacks `{Cursor}` for a scope #4 draft, or it's a `UDS Version: X.Y`
   page outside a release workflow), STOP — do not write.

Required AFTER any node creation:

3. **Validate placement at the page level, not just the node.**
   `node.screenshot()` and a node-scoped `get_screenshot` render the node
   regardless of which page it lives on — a clean node screenshot proves
   the node *renders*, NOT that it's on the right page. Confirm placement
   by reading the created node's page (walk `node.parent` to its `PAGE`, or
   screenshot the destination page) and checking it equals the authorized
   target.
4. **If a node landed on the wrong page, move it and report the slip.**
   `correctPage.appendChild(node)` relocates it (it does not duplicate).
   An unauthorized-page write is a scope breach in the write summary even
   after relocation — report it, never silently fix it.

## Component-edit preflight (before any write to a factory component)

A UDS component carries its own spec and its own version stamp. Editing the
COMPONENT — its variants, structure, or token bindings — is never a
free-form design change, not even on a `{Cursor}` draft.

This preflight governs editing an EXISTING component. Two things it does NOT
gate: a NEW component built from scratch via the factory (scope #4) has no
prior contract or stamp — the factory authors both at the current bar, so
steps 1–3 don't apply; and benign instance overrides (setting a placed
instance's text or picking a variant to assemble an example or demo) are
normal consumption, not a component edit.

Before the FIRST write to a `udc-*` / `_udc-*` component DEFINITION (a main
set, a subpart set, or their variants / bindings / structure) in either UDS
Figma file, you MUST:

1. **Read the governing `<<UDS-FACTORY-CONTRACT>>` block.** It lives on the
   `udc-<id>` MAIN component set. If you are editing a `_udc-*` subpart (e.g.
   `_udc-stepper_step`), it carries NO contract of its own — its description
   is empty. That emptiness is NOT "no rules": resolve the parent — the
   `udc-*` set that INSTANCES this subpart (the nesting graph is
   authoritative; the name is only a hint, since hyphenated ids make
   name-parsing ambiguous) — and read THAT contract. (This is the trap
   behind the stepper miss — the edits went to `_udc-stepper_step`, whose
   empty description hid the `udc-stepper` contract that recorded
   `display-only` and focus `notApplicable`.) If neither the subpart nor its
   parent carries a contract — a pre-versioning draft — there's no recorded
   decision to honor; steps 2–5 still apply, and step 5 authors the contract
   on first touch. Decisions recorded in the contract — `display-only`, a
   `notApplicable` state, a token-tone binding, an axis convention — are
   BINDING. A change that contradicts one is a PROPOSAL to the user, not a
   free edit, even on a free-write draft. Stop and surface it; do not just
   apply it.
2. **Read the current factory bar** — `version` in
   `.cursor/figma/state/factory-version.json`.
3. **Read the component's stamped version** — the `factory_version` shared
   plugin data on the `udc-<id>` main set (and its `Factory-version:`
   contract line) — and compare it to the bar:
   - **At bar** — proceed with the scoped edit.
   - **Behind bar** — the edit is a MAINTENANCE UPGRADE governed by
     `.cursor/rules/uds-factory-versioning.mdc`, NOT a free design session. A
     newer bar is not a license to change everything. Pause and give the user
     the scope fork: **(A)** make ONLY the change they asked for, leave the
     component at its old version, keep it flagged behind, and report what a
     full upgrade to the bar would entail; or **(B)** fold the change into a
     full to-bar upgrade (inspect → diff → propose every change → per-change
     approval). Do NOT auto-pick B. Do NOT sweep bar-upgrade work into a
     change the user scoped narrowly.
4. **Edit through the factory's own flow** — the
   `generate-uds-figma-component` factory skill (build mode for a draft,
   maintenance mode for a live component) and the named sync skills encode
   the contract round-trip. The hazard isn't the `use_figma` tool itself;
   it's making a component change OUTSIDE the preflight + contract-
   regeneration + honest-stamp discipline.

Required AFTER any write to a component:

5. **Regenerate the WHOLE contract block** on the `udc-<id>` main set from
   the component's current anatomy (never patch a single line). Then keep the
   version stamp HONEST. The stamp records the bar the component MEETS
   (`uds-factory-versioning.mdc`), so never advance it to a bar the component
   does not meet:
   - A scoped fix that leaves the component behind: regenerate the contract
     (it's a derived view), but leave `Factory-version:` and the
     `factory_version` plugin data at the old version and report it still
     behind.
   - A full to-bar upgrade: re-stamp to the new bar, per
     `uds-factory-versioning.mdc` (which owns the re-stamp discipline).

   Re-stamping to a bar the component does not meet is PROHIBITED — it makes
   the component claim a standard it fails. (This rule exists because a
   stepper draft was edited yet left stamped `2026.06.08.1`, its contract
   describing a component that no longer existed.)

Page permission and component currency are independent. The `{Cursor}`
standing write grant (scope #4) authorizes WHERE you may write; it never
exempts you from this preflight. See
`.cursor/rules/uds-factory-versioning.mdc` (the maintenance loop + re-stamp
discipline) and `.cursor/rules/uds-figma-factory-quality.mdc` (the output bar
each version encodes).

## Allowed write scopes

The only routine Figma write scopes are:

1. **Release Notes frames**
   - Remove and rebuild only the frame named `Release Notes`
   - Target page must be the current `UDS Version: X.Y` page
   - Both UDS Tokens and UDS Components release-note frames must contain
     identical text if syncing release notes

2. **Component status page-name prefixes**
   - Allowed only when the user explicitly asks to sync status
   - Must report old page name and new page name
   - Do not infer status from spec completeness; status comes from Figma
     stoplight prefix or explicit user direction

3. **Cover-page version text**
   - Allowed only during a confirmed release workflow
   - Must report old text and new text

4. **Component drafts on a `{Cursor}{Ignore}` page in `UDS Components`**
   - Allowed only when the user explicitly invokes the
     `generate-uds-figma-component` factory skill (or equivalent
     prompt: "factory me an X", "generate a UDS component for Y") and
     names the target component title.
   - Target page MUST be a page in `UDS Components` whose name
     contains `{Cursor}` — typically `🟠 <Title> {Cursor}{Ignore}`
     (new or existing). The `{Ignore}` marker takes the page out of
     every UDS automation per `.cursor/rules/uds-figma-preflight.mdc`;
     the `{Cursor}` label marks it as Cursor's working draft.
   - A draft page may host a **family** — several public `udc-<stem>...`
     member sets of one component on the one stem-named page (e.g.
     `udc-data-field` + `udc-data-field-group`). That's expected, not a
     scope breach: the whole `{Cursor}` page is the write target. See
     `.cursor/rules/uds-naming-conventions.mdc` §8.
   - **The `{Cursor}` tag is a standing write grant.** Any page whose
     name contains `{Cursor}` is Cursor's to create, modify, rebuild,
     or delete nodes on — freely, without per-action permission —
     until the user removes the `{Cursor}` tag. Removing `{Cursor}`
     (the designer's rename) revokes the grant; after that the page is
     designer-owned and writes need explicit per-target direction.
   - **Permission is not exemption.** The `{Cursor}` grant covers WHERE you
     write, not freedom from the "Component-edit preflight" above: even on a
     free-write draft you must read the component's contract + the current
     factory bar, treat recorded decisions as binding, regenerate the
     contract on any write, and keep the version stamp honest.
   - Writes outside that page (other component pages, the cover, the
     release-notes frame, `_support` / `📗 Cover` / `🎬 Demo` /
     `🛝 Playground` pages) are NOT covered by this scope and still
     require explicit per-target user direction.
   - Designer accepts a draft by RENAMING the page (drop
     `{Cursor}{Ignore}`, set the stoplight prefix). The factory must
     not auto-rename — that's the acceptance gesture.
   - If a `<Title> {Cursor}{Ignore}` page already exists for the same
     component, inspect it first to decide whether to resume/extend or
     rebuild — but because the `{Cursor}` tag is present, permission to
     rebuild is NOT required. The factory must still never auto-remove
     the `{Cursor}` / `{Ignore}` tags (that rename is the designer's
     acceptance gesture).

5. **Factory maintenance upgrades to a live component page**
   - Allowed only when the user explicitly directs a factory upgrade /
     maintenance run for a named live component (e.g. "upgrade
     metric-card to the current factory bar"), or confirms a specific
     change from a `figma-inventory` factory-version drift report. This
     is NOT a standing grant.
   - Target MUST be a LIVE component page in `UDS Components` — no
     `{Cursor}`, no `{Ignore}`. (Draft writes are scope #4 and are free;
     this scope is the gated, accepted-component path.)
   - **Per-change approval, never silent.** Every change is proposed and
     applied only on the user's explicit OK of that specific change —
     additive and breaking alike. Nothing auto-applies on a live page.
     The factory writes ONLY the approved scoped change, never a free
     rebuild.
   - A designer hand-edit (the live component differs from what the
     factory would produce) is surfaced, never overwritten.
   - A page tagged `{Frozen}` / `{NoFactory}` is excluded — reported as
     behind, never written (see `.cursor/rules/uds-figma-preflight.mdc`).
   - Every write emits the standard before/after/rollback summary.
   - Driven by the `generate-uds-figma-component` maintenance mode and
     `.cursor/rules/uds-factory-versioning.mdc`.

All other Figma writes require a direct user instruction that names the
target object or operation.

## Required before/after report

For every Figma write, return:

```markdown
## Figma write summary

- File: UDS Tokens | UDS Components
- File key: ...
- Page: ...
- Node/frame: ...
- Operation: create | update | delete | rename
- Old value: ...
- New value: ...
- Rollback note: ...
```

For any write to a `udc-*` / `_udc-*` component, ALSO return a
factory-currency block. You cannot fill it honestly without having run the
Component-edit preflight, so a blank or hand-waved one is the visible tell
that the preflight was skipped:

```markdown
## Factory currency

- Current factory bar: <YYYY.MM.DD.N from factory-version.json>
- Component stamp before: <version>
- Component stamp after: <version — unchanged unless the component now meets the bar>
- Contract block regenerated: yes/no
- Contract decisions: respected | changed-with-user-approval (<which>)
- Still behind bar: yes/no (<what a later upgrade would entail, if behind>)
```

If a write fails halfway through, stop and report the exact last successful
operation. Do not continue with more writes until the user reviews the state.

## Destructive operations

Never auto-delete components, variables, component sets, pages, or library
assets in Figma.

The only allowed deletion-like routine operation is deleting the existing
`Release Notes` frame immediately before rebuilding it during release-note
sync.

## Related workflows

- `.cursor/rules/uds-figma-preflight.mdc`
- `.cursor/rules/uds-figma-change-classification.mdc`
- `.cursor/skills/sync-figma-release-notes/SKILL.md` — uses scope #1
- `.cursor/skills/sync-figma-component-status/SKILL.md` — uses scope #2
- `.cursor/skills/generate-uds-figma-component/SKILL.md` — uses scope #4
  (component drafts on `{Cursor}{Ignore}` pages) and scope #5 (gated
  upgrades to live components, per `.cursor/rules/uds-factory-versioning.mdc`)
- `.cursor/rules/uds-factory-versioning.mdc` — defines the build version
  whose drift triggers scope #5 maintenance upgrades and the Component-edit
  preflight's behind-bar path; owns the re-stamp + contract-regeneration
  discipline
- `.cursor/rules/uds-figma-factory-quality.mdc` — the per-version output bar
  the Component-edit preflight checks a component against
